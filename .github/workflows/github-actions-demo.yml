name: Deploy to AWS 

on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: [ ubuntu-latest, windows-latest ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '7.0'
          
      - name: Build and test
        run: |
          npm run build
          npm test -- --runInBand
